{% extends 'gallery_base.html.twig' %}

{% block body %}
    <h3>Groups creation</h3>
	

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="CREATE"  class="btn btn-primary"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('groups_index') }}">Back to the list</a>
        </li>
		
    </ul>
			

{% endblock %}
